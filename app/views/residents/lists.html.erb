<%= render "shared/second-header" %>
<%= render "shared/admin-header" %>

<div class="applicant-lists-page">
  <div class="applicant-lists-title">
    <h3><i class="fa-solid fa-rectangle-list" style="margin-right: 20px;"></i>入居者リスト</h3>
  </div>
  <div class="applicant-lists">
    <table class="applicant-lists-table">
      <thead class="applicant-lists-heading">
        <tr>
          <th></th>
          <th>ユーザー名</th>
          <th>名前</th>
          <th>メールアドレス</th>
          <th>電話番号</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @user.each do |resident| %>
          <tr class="applicant-lists-r">
            <td><i class="fa-solid fa-id-badge"></i></td>
            <td><%= resident.nickname %></td>
            <td><%= resident.individual.last_name  + "    " + resident.individual.first_name %></td>
            <td><%= link_to "<i class='fa-regular fa-envelope' style='margin-right: 10px;'></i>#{resident.email}".html_safe, "#" %></td>
            <td><%= link_to "<i class='fa-solid fa-square-phone' style='margin-right: 10px;'></i>#{resident.individual.phone_number}".html_safe, "#" %></td>
            <td><%= link_to "<i class='fa-solid fa-user-pen'></i>".html_safe, edit_user_path(resident.id) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<%= render "shared/footer" %>
